<%= form_for(@priority_condition) do |f| %>
  <% if @priority_condition.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@priority_condition.errors.count, "error") %> prohibited this priority_condition from being saved:</h2>

      <ul>
      <% @priority_condition.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
